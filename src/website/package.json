{
  "name": "src-website",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "generate:playground-fixtures": "node --import tsx scripts/generate-playground-fixtures.mts",
    "generate:release-notes": "node --import tsx scripts/generate-release-notes.mts",
    "generate:supported-resources": "node --import tsx scripts/generate-supported-resources.mts",
    "prestart": "node --import tsx scripts/generate-playground-fixtures.mts && node --import tsx scripts/generate-release-notes.mts",
    "prebuild": "node --import tsx scripts/generate-playground-fixtures.mts && node --import tsx scripts/generate-supported-resources.mts && node --import tsx scripts/generate-release-notes.mts && node --import tsx scripts/generate-plain-text.mts && node --import tsx scripts/generate-search-index.mts",
    "pretypecheck": "node --import tsx scripts/generate-playground-fixtures.mts",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "postbuild": "node --import tsx scripts/generate-search-index.mts --cleanup && node --import tsx scripts/generate-plain-text.mts --cleanup",
    "dev": "node --import tsx scripts/dev.mts",
    "preview": "npm run build && node --import tsx server.mts",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@docusaurus/core": "3.9.2",
    "@docusaurus/preset-classic": "3.9.2",
    "@mdx-js/react": "3.0.0",
    "@resvg/resvg-js": "2.6.2",
    "clsx": "2.0.0",
    "express": "5.1.0",
    "prism-react-renderer": "2.3.0",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "terminal-image": "^2.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.9.2",
    "@docusaurus/tsconfig": "3.9.2",
    "@docusaurus/types": "3.9.2",
    "chokidar": "3.6.0",
    "http-proxy-middleware": "2.0.9",
    "tsx": "4.21.0",
    "typescript": "5.6.2"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  }
}
